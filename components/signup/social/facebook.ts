export function getFacebookAuthNotWiredMessage() {
  return "Facebook auth SDK is not wired yet. Once Facebook SDK login is added, we will send its provider token to /v1/auth/social automatically.";
}
