"use client";

import { BadgeCheck, ChevronDown, CircleDollarSign, FileCheck2, MapPin, UploadCloud, UserRound } from "lucide-react";
import { getCountries, getCountryCallingCode, parsePhoneNumberFromString, type CountryCode } from "libphonenumber-js";
import { useRouter } from "next/navigation";
import { useState } from "react";

import { getAccessToken, useSessionUser } from "../../auth/authSession";
import { OnboardingNavbar } from "../../signup/OnboardingNavbar";
import { StepTwoAddressConfirm } from "../../signup/profile-setup/StepTwoAddressConfirm";
import { StepTwoLocationPrompt } from "../../signup/profile-setup/StepTwoLocationPrompt";
import { TutorReviewStep } from "./TutorReviewStep";
import { useTutorLocationSetup, type TutorLocationSummary } from "./useTutorLocationSetup";

type Stage = "overview" | "personal" | "identity" | "compensation" | "location" | "review";
type SetupStage = Exclude<Stage, "overview" | "review">;
type OperatingCountry = "NG" | "GB" | "";
type PersonalForm = { firstName: string | null; lastName: string | null; otherName: string; dateOfBirth: string; email: string | null; phoneNumber: string; country: OperatingCountry; occupation: string; qualification: string; experience: string };
type CompensationForm = { bankName: string; accountName: string; firstName: string; lastName: string; accountNumber: string; sortCode: string };

const stages = [
  { id: "personal", label: "Personal information", icon: UserRound },
  { id: "identity", label: "Identification verification", icon: FileCheck2 },
  { id: "compensation", label: "Compensation details", icon: CircleDollarSign },
  { id: "location", label: "Location access", icon: MapPin },
] as const;
const fieldClassName = "mt-1.5 h-11 w-full rounded-lg border border-[#d8dde8] bg-white px-3.5 text-sm font-medium text-[#46506a] outline-none focus:border-[#6d63ee] focus:ring-2 focus:ring-[#6d63ee]/15 aria-[invalid=true]:border-brand-danger";
const regionNames = new Intl.DisplayNames(["en"], { type: "region" });
const phoneCountries = getCountries().map((iso) => ({ callingCode: `+${getCountryCallingCode(iso)}`, country: regionNames.of(iso) ?? iso, iso })).sort((a, b) => a.country.localeCompare(b.country));

function FieldLabel({ children, optional = false }: { children: React.ReactNode; optional?: boolean }) {
  return <span className="text-xs font-semibold text-[#596277]">{children}{optional ? <span className="font-medium text-[#9299a9]"> (optional)</span> : <span className="text-brand-danger"> *</span>}</span>;
}

function SelectField({ ariaInvalid, onChange, options, placeholder, value }: { ariaInvalid?: boolean; onChange: (value: string) => void; options: readonly { label: string; value: string }[]; placeholder: string; value: string }) {
  return <span className="relative mt-1.5 block"><select aria-invalid={ariaInvalid} className="h-11 w-full appearance-none rounded-lg border border-[#d8dde8] bg-[#fbfcff] px-3.5 pr-11 text-sm font-semibold text-[#46506a] shadow-[0_1px_2px_rgba(25,32,56,0.04)] outline-none transition hover:border-[#bfc5d3] focus:border-[#6d63ee] focus:bg-white focus:ring-2 focus:ring-[#6d63ee]/15 aria-[invalid=true]:border-brand-danger" onChange={(event) => onChange(event.target.value)} value={value}><option value="">{placeholder}</option>{options.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select><span className="pointer-events-none absolute inset-y-0 right-3.5 flex items-center border-l border-[#e4e7ee] pl-3 text-[#6864d4]"><ChevronDown aria-hidden className="h-4 w-4" /></span></span>;
}

function StepList({ active, completed, onSelect }: { active: SetupStage; completed: SetupStage[]; onSelect: (stage: SetupStage) => void }) {
  const activeIndex = stages.findIndex((step) => step.id === active);
  return <aside className="hidden w-[18rem] shrink-0 lg:block"><div className="space-y-2">{stages.map((step, index) => { const Icon = step.icon; const done = completed.includes(step.id); const current = index === activeIndex; const selectable = done || current; return <button className={`flex min-h-12 w-full items-center justify-between rounded-xl border px-3.5 text-left ${current ? "border-[#cfd3fa] bg-[#f1f3ff]" : "border-[#e0e5ee] bg-[#f7f9fc]"} ${selectable ? "cursor-pointer" : "cursor-default"}`} disabled={!selectable} key={step.id} onClick={() => onSelect(step.id)} type="button"><span className="flex items-center gap-2.5 text-xs font-semibold text-[#39435b]"><span className="inline-flex h-6 w-6 items-center justify-center rounded-full bg-[#4039bd] text-white"><Icon size={13} /></span>{step.label}</span>{done ? <BadgeCheck className="h-4 w-4 text-[#20bca8]" /> : <span className={`h-4 w-4 rounded-full ${current ? "bg-[#7d8495]" : "border border-[#b8c0d0]"}`} />}</button>; })}</div></aside>;
}

export default function TutorOnboardingFlow() {
  const router = useRouter();
  const sessionUser = useSessionUser();
  const [stage, setStage] = useState<Stage>("overview");
  const [completed, setCompleted] = useState<SetupStage[]>([]);
  const [returnToReview, setReturnToReview] = useState(false);
  const [validationVisible, setValidationVisible] = useState(false);
  const [identityValidationVisible, setIdentityValidationVisible] = useState(false);
  const [compensationValidationVisible, setCompensationValidationVisible] = useState(false);
  const [reviewConfirmed, setReviewConfirmed] = useState(false);
  const [submissionMessage, setSubmissionMessage] = useState("");
  const [personalForm, setPersonalForm] = useState<PersonalForm>({ firstName: null, lastName: null, otherName: "", dateOfBirth: "", email: null, phoneNumber: "", country: "", occupation: "", qualification: "", experience: "" });
  const [phoneCountry, setPhoneCountry] = useState<CountryCode>("NG");
  const [shareCode, setShareCode] = useState("");
  const [dbsNumber, setDbsNumber] = useState("");
  const [idType, setIdType] = useState("");
  const [idFile, setIdFile] = useState<File | null>(null);
  const [fileError, setFileError] = useState("");
  const [compensation, setCompensation] = useState<CompensationForm>({ bankName: "", accountName: "", firstName: "", lastName: "", accountNumber: "", sortCode: "" });
  const [locationSummary, setLocationSummary] = useState<TutorLocationSummary | null>(null);

  const firstName = personalForm.firstName ?? sessionUser?.firstName ?? "";
  const lastName = personalForm.lastName ?? sessionUser?.lastName ?? "";
  const email = personalForm.email ?? sessionUser?.email ?? "";
  const phoneValid = parsePhoneNumberFromString(personalForm.phoneNumber.trim(), phoneCountry)?.isValid() ?? false;
  const phoneDisplay = parsePhoneNumberFromString(personalForm.phoneNumber.trim(), phoneCountry)?.formatInternational() ?? personalForm.phoneNumber;
  const personalComplete = Boolean(firstName.trim() && lastName.trim() && personalForm.dateOfBirth && email.trim() && phoneValid && personalForm.country && personalForm.occupation.trim() && personalForm.qualification.trim() && personalForm.experience.trim().length >= 10);
  const identityComplete = Boolean(idType && idFile && (personalForm.country !== "GB" || (shareCode.trim() && dbsNumber.trim())));
  const compensationComplete = personalForm.country === "GB" ? Boolean(compensation.firstName.trim() && compensation.lastName.trim() && compensation.accountNumber.trim().length >= 8 && compensation.sortCode.replace(/\D/g, "").length === 6) : Boolean(compensation.bankName.trim() && compensation.accountName.trim() && compensation.accountNumber.replace(/\D/g, "").length === 10);
  const displayName = firstName || "there";

  const markCompleted = (item: SetupStage) => setCompleted((current) => current.includes(item) ? current : [...current, item]);
  const goNext = (current: SetupStage, next: SetupStage) => { markCompleted(current); setStage(returnToReview ? "review" : next); setReturnToReview(false); };
  const updatePersonal = <K extends keyof PersonalForm>(field: K, value: PersonalForm[K]) => setPersonalForm((current) => ({ ...current, [field]: value }));
  const updateCompensation = (field: keyof CompensationForm, value: string) => setCompensation((current) => ({ ...current, [field]: value }));
  const changeCountry = (country: OperatingCountry) => { if (country !== personalForm.country) { setShareCode(""); setDbsNumber(""); setIdType(""); setIdFile(null); setFileError(""); setCompensation({ bankName: "", accountName: "", firstName: "", lastName: "", accountNumber: "", sortCode: "" }); } updatePersonal("country", country); };
  const location = useTutorLocationSetup((summary) => { setLocationSummary(summary); markCompleted("location"); setStage("review"); setReturnToReview(false); });

  const handleFile = (file?: File) => { setFileError(""); if (!file) return; if (!["application/pdf", "image/jpeg", "image/png"].includes(file.type)) { setIdFile(null); setFileError("Upload a PDF, JPG, or PNG document."); return; } if (file.size > 5 * 1024 * 1024) { setIdFile(null); setFileError("The document must be 5 MB or smaller."); return; } setIdFile(file); };
  const startSetup = () => { if (!getAccessToken()) router.push("/login"); else setStage("personal"); };
  const continuePersonal = () => { setValidationVisible(true); if (!personalComplete) return; setValidationVisible(false); goNext("personal", "identity"); };
  const continueIdentity = () => { setIdentityValidationVisible(true); if (!identityComplete) return; if (personalForm.country === "GB") setCompensation((current) => ({ ...current, firstName: current.firstName || firstName, lastName: current.lastName || lastName })); setIdentityValidationVisible(false); goNext("identity", "compensation"); };
  const continueCompensation = () => { setCompensationValidationVisible(true); if (!compensationComplete) return; setCompensationValidationVisible(false); goNext("compensation", "location"); };
  const editReview = (section: SetupStage) => { setReviewConfirmed(false); setReturnToReview(true); setStage(section); if (section === "location") location.setView(locationSummary ? "edit" : "prompt"); };

  const activeSetupStage = stage !== "overview" && stage !== "review" ? stage : null;
  const stageIndex = activeSetupStage ? stages.findIndex((item) => item.id === activeSetupStage) : -1;

  return <main className="flex min-h-[100svh] flex-col bg-white text-[#171c2a]">
    <OnboardingNavbar email={sessionUser?.email ?? ""} name={sessionUser?.firstName ?? ""} />
    <section className="flex min-h-0 flex-1 overflow-y-auto px-4 py-4 sm:px-6 lg:px-[var(--dashboard-gutter)] lg:py-3">
      {stage === "overview" ? <div className="mx-auto flex w-full max-w-[34rem] flex-col items-center justify-center text-center"><h1 className="text-2xl font-bold text-[#1d2331] sm:text-3xl">Welcome {displayName}!</h1><p className="mt-2 text-sm font-medium text-[#8a93a7]">Complete your tutor verification to start receiving bookings and payments.</p><div className="mt-6 w-full max-w-[23rem] space-y-2.5 text-left">{stages.map((item) => { const Icon = item.icon; return <div className="flex min-h-12 items-center gap-3 rounded-xl border border-[#d9e4ef] bg-[#edf6fd] px-4 text-xs font-semibold text-[#35405a]" key={item.id}><span className="inline-flex h-6 w-6 items-center justify-center rounded-full bg-[#4039bd] text-white"><Icon size={13} /></span>{item.label}</div>; })}</div><button className="mt-7 h-11 rounded-full bg-brand-primary px-6 text-sm font-semibold text-white" onClick={startSetup} type="button">Let&apos;s get started</button></div>
      : stage === "review" ? <TutorReviewStep compensation={compensation} confirmed={reviewConfirmed} identity={{ shareCode, dbsNumber, idType, file: idFile }} location={locationSummary} onConfirmedChange={(value) => { setReviewConfirmed(value); setSubmissionMessage(""); }} onEdit={editReview} personal={{ firstName, lastName, otherName: personalForm.otherName, dateOfBirth: personalForm.dateOfBirth, email, phone: phoneDisplay, country: personalForm.country, occupation: personalForm.occupation, qualification: personalForm.qualification, experience: personalForm.experience }} />
      : <div className="mx-auto flex w-full max-w-[75rem] items-start justify-center gap-8 xl:gap-16"><StepList active={stage} completed={completed} onSelect={setStage} /><section className="w-full max-w-[42rem]">
        {stage === "personal" ? <><h1 className="text-xl font-bold text-[#252c3c]">Personal information</h1><div className="mt-4 grid grid-cols-1 gap-x-4 gap-y-2.5 sm:grid-cols-2"><label><FieldLabel>First name</FieldLabel><input aria-invalid={validationVisible && !firstName.trim()} className={fieldClassName} onChange={(e) => updatePersonal("firstName", e.target.value)} value={firstName} /></label><label><FieldLabel>Last name</FieldLabel><input aria-invalid={validationVisible && !lastName.trim()} className={fieldClassName} onChange={(e) => updatePersonal("lastName", e.target.value)} value={lastName} /></label><label><FieldLabel optional>Other name</FieldLabel><input className={fieldClassName} onChange={(e) => updatePersonal("otherName", e.target.value)} value={personalForm.otherName} /></label><label><FieldLabel>Date of birth</FieldLabel><input aria-invalid={validationVisible && !personalForm.dateOfBirth} className={fieldClassName} onChange={(e) => updatePersonal("dateOfBirth", e.target.value)} type="date" value={personalForm.dateOfBirth} /></label><label><FieldLabel>Email</FieldLabel><input className={`${fieldClassName} ${sessionUser?.email ? "cursor-not-allowed bg-[#f5f7fb]" : ""}`} onChange={(e) => updatePersonal("email", e.target.value)} readOnly={Boolean(sessionUser?.email)} type="email" value={email} /></label><label><FieldLabel>Phone number</FieldLabel><span className={`mt-1.5 flex h-11 overflow-hidden rounded-lg border bg-white focus-within:border-[#6d63ee] ${validationVisible && !phoneValid ? "border-brand-danger" : "border-[#d8dde8]"}`}><select aria-label="Phone country code" className="w-[7.25rem] bg-[#f7f8fc] px-3 text-xs font-semibold outline-none" onChange={(e) => setPhoneCountry(e.target.value as CountryCode)} value={phoneCountry}>{phoneCountries.map((country) => <option key={country.iso} value={country.iso}>{country.iso} {country.callingCode}</option>)}</select><input className="min-w-0 flex-1 px-3.5 text-sm outline-none" onChange={(e) => updatePersonal("phoneNumber", e.target.value)} placeholder="Phone number" type="tel" value={personalForm.phoneNumber} /></span></label><label className="sm:col-span-2"><FieldLabel>Country where you&apos;ll provide tutoring services</FieldLabel><SelectField ariaInvalid={validationVisible && !personalForm.country} onChange={(value) => changeCountry(value as OperatingCountry)} options={[{ label: "Nigeria", value: "NG" }, { label: "United Kingdom", value: "GB" }]} placeholder="Select country" value={personalForm.country} /></label><label><FieldLabel>Occupation</FieldLabel><input className={fieldClassName} onChange={(e) => updatePersonal("occupation", e.target.value)} value={personalForm.occupation} /></label><label><FieldLabel>Highest qualification</FieldLabel><input className={fieldClassName} onChange={(e) => updatePersonal("qualification", e.target.value)} value={personalForm.qualification} /></label><label className="sm:col-span-2"><FieldLabel>Teaching or tutoring experience</FieldLabel><textarea className="mt-1.5 h-20 w-full resize-none rounded-lg border border-[#d8dde8] px-3.5 py-2.5 text-sm outline-none" maxLength={500} onChange={(e) => updatePersonal("experience", e.target.value)} placeholder="Tell us why you are the perfect tutor for the job." value={personalForm.experience} /><span className="mt-0.5 flex justify-between text-xs text-[#8a93a7]"><span>Bio must be at least 10 characters</span><span>{personalForm.experience.length}/500</span></span></label></div>{validationVisible && !personalComplete ? <p className="mt-3 text-sm font-medium text-brand-danger">Complete all required fields with valid information.</p> : null}</>
        : stage === "identity" ? <><h1 className="text-xl font-bold text-[#252c3c]">Identification verification</h1>{personalForm.country === "GB" ? <div className="mt-5 grid gap-4"><label><span className="mb-2 block border-b border-[#e8ebf2] pb-1 text-xs font-semibold text-[#6f778c]">Right to work (UK)</span><FieldLabel>Employer share code</FieldLabel><input aria-invalid={identityValidationVisible && !shareCode.trim()} className={fieldClassName} onChange={(e) => setShareCode(e.target.value)} placeholder="Enter employer share code" value={shareCode} /><span className="mt-1 block text-xs text-[#8a93a7]">Generate your share code from the UK government website to confirm your right to work.</span></label><label><span className="mb-2 block border-b border-[#e8ebf2] pb-1 text-xs font-semibold text-[#6f778c]">Background check</span><FieldLabel>DBS certificate number</FieldLabel><input aria-invalid={identityValidationVisible && !dbsNumber.trim()} className={fieldClassName} onChange={(e) => setDbsNumber(e.target.value)} placeholder="Enter DBS certificate number" value={dbsNumber} /><span className="mt-1 block text-xs text-[#8a93a7]">Provide your Disclosure and Barring Service (DBS) certificate number for background verification.</span></label></div> : null}<div className="mt-5 grid gap-4"><label><span className="mb-2 block border-b border-[#e8ebf2] pb-1 text-xs font-semibold text-[#6f778c]">Identification type</span><FieldLabel>ID type</FieldLabel><SelectField ariaInvalid={identityValidationVisible && !idType} onChange={setIdType} options={(personalForm.country === "GB" ? ["Passport", "Driver's licence"] : ["NIN", "Passport", "Voter's card", "Driver's licence"]).map((type) => ({ label: type, value: type }))} placeholder="Select ID type" value={idType} /><span className="mt-1 block text-xs text-[#8a93a7]">Accepted: {personalForm.country === "GB" ? "Passport or Driver's Licence" : "NIN, Passport, Voter's Card, or Driver's Licence"}</span></label><label><FieldLabel>Upload ID document</FieldLabel><span className={`mt-1.5 flex min-h-28 cursor-pointer flex-col items-center justify-center rounded-xl border border-dashed bg-[#fbfcff] ${identityValidationVisible && !idFile ? "border-brand-danger" : "border-[#cfd5e2]"}`}><UploadCloud className="h-6 w-6 text-[#5652d2]" /><span className="mt-2 text-sm font-semibold text-[#5652d2]">Click to upload or drag and drop</span><span className="text-xs text-[#9299a9]">PDF, JPG or PNG — maximum 5 MB</span><input accept=".pdf,.jpg,.jpeg,.png" className="sr-only" onChange={(e) => handleFile(e.target.files?.[0])} type="file" /></span></label>{fileError ? <p className="text-sm text-brand-danger">{fileError}</p> : null}{idFile ? <div className="flex justify-between rounded-xl border border-[#d9d7fb] bg-[#faf9ff] px-4 py-3 text-sm"><span>{idFile.name}</span><button className="font-semibold text-brand-danger" onClick={() => setIdFile(null)} type="button">Remove</button></div> : null}</div>{identityValidationVisible && !identityComplete ? <p className="mt-3 text-sm font-medium text-brand-danger">Complete all identification requirements.</p> : null}</>
        : stage === "compensation" ? <><h1 className="text-xl font-bold text-[#252c3c]">Payment details</h1><div className="mt-5 grid grid-cols-1 gap-4 sm:grid-cols-2">{personalForm.country === "GB" ? <><label><FieldLabel>First name</FieldLabel><input aria-invalid={compensationValidationVisible && !compensation.firstName.trim()} className={fieldClassName} onChange={(e) => updateCompensation("firstName", e.target.value)} value={compensation.firstName} /></label><label><FieldLabel>Last name</FieldLabel><input aria-invalid={compensationValidationVisible && !compensation.lastName.trim()} className={fieldClassName} onChange={(e) => updateCompensation("lastName", e.target.value)} value={compensation.lastName} /></label><label><FieldLabel>Account number</FieldLabel><input className={fieldClassName} inputMode="numeric" onChange={(e) => updateCompensation("accountNumber", e.target.value)} value={compensation.accountNumber} /></label><label><FieldLabel>Sort code</FieldLabel><input className={fieldClassName} inputMode="numeric" onChange={(e) => updateCompensation("sortCode", e.target.value)} placeholder="00-00-00" value={compensation.sortCode} /></label></> : <><label><FieldLabel>Bank name</FieldLabel><input className={fieldClassName} onChange={(e) => updateCompensation("bankName", e.target.value)} value={compensation.bankName} /></label><label><FieldLabel>Account number</FieldLabel><input className={fieldClassName} inputMode="numeric" maxLength={10} onChange={(e) => updateCompensation("accountNumber", e.target.value.replace(/\D/g, ""))} value={compensation.accountNumber} /></label><label className="sm:col-span-2"><FieldLabel>Account holder name</FieldLabel><input className={fieldClassName} onChange={(e) => updateCompensation("accountName", e.target.value)} value={compensation.accountName} /></label></>}</div>{compensationValidationVisible && !compensationComplete ? <p className="mt-3 text-sm font-medium text-brand-danger">Enter valid payment details before continuing.</p> : null}</>
        : <>{location.view === "prompt" ? <StepTwoLocationPrompt locationError={location.error} onAllowLocation={location.requestCurrentLocation} onEnterAddress={() => location.openSearch()} requestingLocation={location.requestingLocation} variant="tutor" /> : <StepTwoAddressConfirm addressForm={location.address} coordinates={location.coordinates} locationError={location.error} mode={location.view} onAddressFieldChange={location.changeAddress} onMapLocationChange={location.moveMap} onPlaceQueryChange={location.changeQuery} onSelectPlace={location.selectPlace} placePredictions={location.predictions} placeQuery={location.query} requestingPlaceSearch={location.requestingSearch} resolvingMapLocation={location.resolvingMap} />}</>}
      </section></div>}
    </section>

    {stage !== "overview" ? <footer className="shrink-0 border-t border-[#e5e8f2] bg-white px-4 py-2 sm:px-[var(--dashboard-gutter)]"><div className="mx-auto flex w-full max-w-[var(--dashboard-max-width)] items-center justify-between gap-3"><span className="rounded-full border border-[#6d63ee] px-2.5 py-1 text-xs font-semibold text-[#5b4ded]">{stage === "review" ? "Review" : `Step ${stageIndex + 1}/4`}</span><div className="flex items-center gap-2">{submissionMessage ? <span className="hidden text-xs font-medium text-[#8b5a20] sm:inline">{submissionMessage}</span> : null}<button className="h-11 rounded-full border border-[#d8dde8] bg-white px-5 text-sm font-semibold" onClick={() => { if (returnToReview && stage !== "review") { setReturnToReview(false); setStage("review"); } else if (stage === "review") setStage("location"); else if (stage === "personal") router.push("/students/dashboard"); else if (stage === "identity") setStage("personal"); else if (stage === "compensation") setStage("identity"); else if (location.view === "prompt") setStage("compensation"); else location.goBack(); }} type="button">{stage === "personal" ? "Cancel" : "Back"}</button>{stage === "personal" ? <button className="h-11 rounded-full bg-brand-primary px-6 text-sm font-semibold text-white" onClick={continuePersonal} type="button">Continue</button> : stage === "identity" ? <button className="h-11 rounded-full bg-brand-primary px-6 text-sm font-semibold text-white" onClick={continueIdentity} type="button">Continue</button> : stage === "compensation" ? <button className="h-11 rounded-full bg-brand-primary px-6 text-sm font-semibold text-white" onClick={continueCompensation} type="button">Continue</button> : stage === "location" ? location.view === "prompt" ? <><button className="h-11 rounded-full border border-[#d8dde8] px-5 text-sm font-semibold" onClick={() => { setLocationSummary(null); markCompleted("location"); setStage("review"); setReturnToReview(false); }} type="button">Skip</button></> : location.view === "search" ? null : <><button className="h-11 rounded-full border border-[#d8dde8] px-5 text-sm font-semibold" onClick={location.view === "edit" ? location.goBack : location.openEdit} type="button">{location.view === "edit" ? "Cancel" : "No, edit address"}</button><button className="h-11 rounded-full bg-brand-primary px-5 text-sm font-semibold text-white disabled:bg-[#b8b6cf]" disabled={!location.complete || location.saving || location.resolvingMap} onClick={() => void location.confirm()} type="button">{location.saving ? "Saving..." : "Yes, this is my address"}</button></> : <button className="h-11 rounded-full bg-brand-primary px-6 text-sm font-semibold text-white disabled:bg-[#b8b6cf]" disabled={!reviewConfirmed} onClick={() => setSubmissionMessage("The tutor onboarding submission endpoint is not connected yet.")} type="button">Finish setup</button>}</div></div></footer> : null}
  </main>;
}
